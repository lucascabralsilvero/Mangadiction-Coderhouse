import React from 'react';
import styles from "./Cart.module.scss"

const Cart = () => {
  return (
    <section className={styles.height}>
        <h1 className={`${styles.padding} text-center`}>Cart</h1>
    </section>
  )
}

export default Cart;